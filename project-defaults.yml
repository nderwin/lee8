logger:
  level: DEBUG
swarm:
  datasources:
    jdbc-drivers:
      org.postgresql:
        driver-class-name: org.postgresql.Driver
        xa-datasource-class-name: org.postgresql.xa.PGXADataSource
        driver-module-name: org.postgresql
    data-sources:
      lee8:
        driver-name: org.postgresql
        connection-url: jdbc:postgresql://database:5432/lee8db
        # These need to be runtime injected, or Docker secrets, or something...
        user-name: lee8
        password: 8eel
management:
  security-realms:
    ManagementRealm:
      in-memory-authentication:
        users:
          admin:
            password: admin1admin!
  http-interface-management-interface:
    allowed-origins:
     - http://localhost:8080
    security-realm: ManagementRealm
